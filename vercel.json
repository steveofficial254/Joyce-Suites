{
    "builds": [
        {
            "src": "joyce-suites/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "build"
            }
        }
    ]
}